# Copy this file to backend/.env and fill in your Stripe keys
# Do NOT commit real keys to version control.

# Stripe test keys (example placeholders)
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# If you're running in production, set real keys as environment variables instead of files.
